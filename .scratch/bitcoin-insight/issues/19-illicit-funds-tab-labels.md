# 자금추적 탭 라벨만 번호 접두사를 쓴다

Status: resolved
Section: S3
Tags: 일관성

`① 현금 vs 체인` / `② 세탁 경로 추적` / `③ 실제 사례`. 인사이트 그룹에서 번호 접두사를 쓰는 유일한 페이지였다. 탭은 순서를 강제하지 않으므로 번호는 없는 순서를 있는 것처럼 보이게 한다.

## Answer

접두사를 걷어냈다.

## Comments

프로토콜 그룹의 `/chain-reorg`도 같은 방식(`① 재구성 경주` 등)을 쓴다. 이번 정비 범위 밖이라 손대지 않았다. 그 그룹을 정비할 때 함께 처리할 것.
